import type { Testimonial } from "@/types/content";

/**
 * PLACEHOLDER POLICY: no fabricated reviews, ratings, or quotes.
 * This array stays empty until Dante/leadership supply authentic,
 * approved testimonials (see CONTENT_APPROVALS.md, "Authentic testimonials").
 * The /reviews page and ReviewSection component are written to render an
 * honest empty state, not a fake list, when this is empty.
 */
export const testimonials: Testimonial[] = [];
